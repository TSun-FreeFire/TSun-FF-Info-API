services:
  tsun-ff-info-api:
    build: .
    container_name: tsun-ff-info-api
    restart: always
    ports:
      - "127.0.0.1:8075:8075"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8075/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
